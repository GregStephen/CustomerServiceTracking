import React from 'react';
import PropTypes from 'prop-types';

class AssignedJob extends React.Component {
  static propTypes = {
    job: PropTypes.object,
  }

  render() {
    return (
      <div className="AssignedJob">

      </div>
    );
  }
}

export default AssignedJob;
